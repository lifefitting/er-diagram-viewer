# Changelog

本文件记录 ER Diagram Viewer 的重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/)，
版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.3.3] — 2026-07-31

从单个评审现场走向可合并、可精调、可安全传递的多工作区评审流程。

### 新增

- **布局精准操控**：`Alt` 拖拽仅水平移动，`Shift` 拖拽仅纵向四分之一速度
  精调，连接端口近似水平时自动吸附并给出拉直正反馈。多选表增加以最宽表为准
  的批量等宽、左/中/右/上/中/下对齐、水平/垂直均匀分布，操作会重算连线、
  持久化布局并作为一次撤销历史。
- **搜索范围与读编模式**：左上角搜索支持「全部 / 表名 / 字段」三种范围；
  阅读模式用蓝色状态锁定布局，编辑模式用琥珀色状态开放布局与关系编辑。
- **工作区密码加密**：导出 `.erreview` 前设置密码，使用 PBKDF2-SHA-256 派生密钥、
  AES-GCM 加密完整工作区（SQL / 决策 / 批注 / 布局）。导入自动识别加密文件并解锁，
  同时继续支持旧版未加密存档。
- **模块批量调整**：多选表可一次移入指定业务模块，也可恢复自动分组，
  用人工评审结果纠正不准确的自动聚类。
- **数据库评审记录与标准说明书**：表删除从匿名回收站状态升级为带操作时间的
  「标记删除」评审决策，表头可直接标记，删除建议列表可取消；评审报告新增表级
  决策表，字段意见统一导出为「时间 / 表 / 字段 / 评审建议」表格（级别和状态保留
  在建议单元格）。字段批注增加命名、类型、索引、非空、注释、冗余六类一键常用
  建议，点击即保存。数据库说明文档升级为正式《数据库设计说明书》结构：文档控制、
  目的与范围、设计约定、数据库概览、完整数据字典、约束索引、表关系及术语附录。
- **多工作区存档合并导入**：工作区存档面板支持一次选择或拖入多个 `.erreview`
  文件，先完成整组合并预检再原子替换当前工作区。每个来源作为刚性布局组保留原有
  相对位置与手工路由；仅在组间发生碰撞时整体做最短轴向平移。业务键推断和模块
  配色按来源隔离，避免合并 SQL 后产生跨工作区误连；检测到同名表或不可安全合并
  的评审状态时取消导入。视图菜单新增按来源快速聚焦，初次打开自动显示合并全貌。
- **批注结构化（roadmap 2.2）**：字段批注升级为带**级别**（建议 / 警告 / 阻塞）
  与**状态**（待处理 / 已采纳 / 不采纳）的结构化评审意见——多轮评审需要状态
  流转，自由文本承载不了「哪些已处理」。批注气泡新增级别快捷选择（编辑已有
  批注时再显示状态行——新发现天然是待处理）；画布行标记点按级别着色（琥珀 /
  橙 / 玫红）；右侧「评审建议」浮层按 待处理优先 → 级别 → 时间 排序，头部
  分级计数，行内状态徽章**点击即流转**（待处理 → 已采纳 → 不采纳，不改写
  「写于」时间戳）；评审报告的字段意见按级别分组、行首标状态、开头给
  级别×状态汇总。旧批注（含存档/刷新恢复）自动升级为 建议/待处理。
- **工作区存档（roadmap 1.1）**：评审是多轮的，sessionStorage 关标签即丢——
  现在可以把整个评审现场导出为单个 `.erreview` 文件（导出菜单「工作区存档」），
  内容 = 持久化状态快照（SQL + 决策 + 手动连线 + 业务键选择 + 字段批注 + 布局 +
  相机视角 + 回收站）+ 元信息（格式/应用版本、导出时间、表数）。在导入对话框
  上传或拖入 `.erreview`（按内容识别，改名成 .json/.txt 也认），显示存档摘要
  （导出时间 / 表数 / 决策 / 连线 / 批注数）后一键「导入存档」**完整恢复评审
  现场**——布局与视角逐像素还原（导入即重放刷新恢复路径）。校验复用
  sessionStorage 同款逐字段形状校验：坏字段回退默认值、版本不兼容降级为仅
  恢复 SQL、解析预检失败不动当前工作区；主题等个人偏好不随存档覆盖。
  存档文件同时是最小协作载体：发给同事即可接续评审。
- **「沉稳」模块配色（新默认，id `professional`）**：为评审场景设计的低饱和 12 色——
  OKLCH 空间取色并机器校验（明度带 / 彩度下限 / 色觉障碍模拟分离 / 对比度），
  不靠肉眼：表头白字全部 ≥ 4.5:1（WCAG AA），槽位顺序按 CVD 可分性优化
  （最大的模块拿到最可分的颜色），蓝色（denim）领衔。**明暗双模式各自校验**：
  暗色画布不再依赖自动 HSL 提亮（会把深青炸成霓虹色），而是每槽自带手工
  分阶的暗色边线变体（`ModuleColor.headerDark`，蓝紫家族额外按明度分层——
  紫色在红绿色盲下与蓝色坍缩，只能靠明度区分）。设计约束、验证数值与已知
  极限记录在 [docs/palette-professional.md](docs/palette-professional.md)。
  既有四套配色不变，仍走自动提亮回退；已持久化的配色选择不受影响。

- **连线框选批量删除**：空白处框选现在也能圈中手动连线——一次框选只产生一种
  选择：碰到任何卡片即为表选择；一张卡都没碰到时 rubber-band 框内/穿框的连线
  （触碰即选中，与表同语义）。Delete 有选中连线时只删连线、绝不动表。
- **评审报告导出选项**：导出菜单中「评审报告」展开二级选项，可分别选择是否包含
  推断外键候选 / 逻辑关联候选（手动连线始终包含）；省略的章节在报告开头注明。

### 变更

- **导入对话框双模式重构**：「导入 SQL 脚本」与「导入工作区存档」此前混在
  同一个流程里难以区分，现拆为显式的两个入口 tab——**SQL 脚本**（原有
  粘贴 / 上传 / 样例流程）与**工作区存档**（专属面板：空态拖放区 + 选中后的
  存档摘要卡〔文件名、导出时间、应用版本、表 / 决策 / 连线 / 批注计数、降级
  警告〕+ SQL 只读预览）。两个 tab 状态独立互不干扰；拖放 / 上传仍按**内容**
  自动路由到对应 tab（存档文件落在存档页、SQL 落在脚本页），用户始终看得到
  文件去了哪里。
- **配色切换入口改为调色盘图标按钮**：工具栏不再平铺一组色条，改为与主题
  切换一致的图标按钮（悬停提示当前色系）；各配色的色条预览保留在下拉选项里。
- **配色命名收敛**：统一为克制的双字调性词——沉稳 / 明快 / 柔和 / 大地 / 单色
  （原「鲜艳」「粉彩」等命名弃用；持久化 id 不变）。
- **暗色模式对比度修复**：暗色下主按钮是浅色底（inkd-700/800），一批仍写死
  `text-white` 的按钮/图标（导出报告、解析并绘制、导入对话框图标、错误页刷新、
  ⌘↵ 快捷键角标）在暗色下白字浅底不可读，统一改为暗色文字；主导出按钮 hover
  时错误的 `dark:hover:text-white` 一并移除。
- **连线触点交互打磨**：呼吸光晕显著增强（更大半径 + 透明度脉动）；触点热区扩大到
  22px（靠近即可命中的"吸附半径"），hover 时呼吸停止、光晕常亮并放大 1.35 倍——
  磁性吸附反馈；光标改为状态机：hover 触点时是自定义"拉线"光标（一颗小圆点拖出带箭头的弧线，宣告意图），
  开始拖线后切回普通箭头——线已经跟着鼠标走了，光标退位让路，直到落到目标点。
- **拖线预览恢复为平滑曲线**：绘制过程用贝塞尔曲线（顺滑跟手），松手成形后才是
  正交折线——「过程平滑、结果规整」。v0.3.1 曾把预览也改成折线，属理解偏差，撤回。
- **Landing 重塑**：宣传口径转向「数据库设计评审工作台」（标题 / meta / hero /
  CTA 文案全面更新）；新增横向**版本时间轴**（v0.1.0 → v0.3.1，讲述「看清 →
  好用 → 评审闭环」的演进）；差异化对比表扩展 5 个评审维度（业务键逻辑关联、
  字段拖线补关系、字段批注、评审报告、说明文档）。
- **文档收编**：TODO-fix-bugs\*.md 删除（路由统一「不做」结论与 side-bracket
  残留债务并入 roadmap）；roadmap.md 与 handoff.md 移至 docs/ 并确立分工——
  roadmap 记录接下来要做的事，handoff 记录每轮完成后的状态与交接。

### 计划中

- 产品方向四阶段规划（工作区存档、schema diff、规范检查等）见
  [docs/roadmap.md](docs/roadmap.md)；交接与工程债务记录见
  [docs/handoff.md](docs/handoff.md)（原 TODO-fix-bugs 已并入并删除）。
- side-bracket 泛化（`gapX < 0` 限制）。
  ——原「布线引擎统一」条目经复核**决定不做**：导出与画布本就同源（都读
  `routePoints`），双路径各有分工（静态走 dagre 通道、交互走实时绕行）。

## [0.3.1] — 2026-07-11

从「画得清楚」走向「评得下去」：本版把工具补成完整的 **DB 设计评审闭环**——
手动连线补齐推断盲区、业务键逻辑关联表达分库分表关系、字段批注沉淀评审过程，
最终导出评审报告与标准格式的数据库说明文档。

### 新增

- **手动连线（画布直连，连完即所得）**：推断引擎覆盖不到的关系现在可以直接画——
  悬停字段行，两侧出现带琥珀呼吸荧光的连线触点，从任一侧拖出 H-V-H 正交折线
  （按下即出现、带箭头实时跟随，悬停目标行按可连性亮绿/红，悬停同表字段弯成同侧
  U 形预览），**松手立即建立关系、无需逐次确认**：目标是主键/唯一列 → 物理外键，
  否则 → 逻辑关联。方向严格等于绘制方向（拖线起点 = FK 持有方），起手侧随连线
  持久化（`drawSide`，决定同表回环从哪侧鼓出）。
  - **可选可删（支持批量）**：点击连线（含同表回环）选中，`Shift/⌘/Ctrl` 点击加选，
    底部操作条显示「已选 N 条」，Delete/Backspace 批量移除，Esc / 点空白取消——
    与表的多选删除同一套操作习惯。
  - **「手动连线」面板**：记录每条手动连线，逐行「物理/逻辑」类型切换（保留绘制
    方向、校验键冲突）、逐条移除；下拉表单折叠在「不方便拖线？用表单添加…」后。
  - 手动物理外键实线渲染、参与布局权重、进 DDL 导出；与显式/推断外键同路径重复
    添加会被拒绝并提示（保护手改走线的路由键稳定性）。持久化到 sessionStorage
    （导入新 SQL 时清空，刷新保留）。

- **逻辑关联（业务键关联）**：分库分表场景下表之间通过共享业务键（如 `out_trade_no`
  外部交易号）关联而非物理外键，现在成为一等公民：
  - **用户触发的推断（不在导入时自动跑）**：大 DDL 里同名字段太多，全量自动推断会把
    画布搞乱。侧栏「逻辑关联」区块提供「扫描业务键」：列出跨表共享的候选列簇
    （表数、唯一索引侧；无唯一侧且表过多的簇禁选并说明原因），**由用户勾选用哪些
    字段推断**；选中的字段名持久化（刷新按同样选择重推、可随时重新扫描/移除），
    导入新 SQL 时清空。生成规则：有唯一侧连星形（中置信度），否则小簇两两配对
    （低置信度）；`id`/`created_at`/`tenant_id` 等 38 个通用列名内置黑名单。
  - **无向渲染**：点状线 + 两端圆点（画布与 SVG/PNG 导出一致），待定候选降透明度，
    悬停提示用 `~` 分隔符；存储保留绘制方向，仅决策/路由键做无向归一。
  - **不干扰结构**：逻辑边不参与模块聚类（业务键横跨领域，不该把订单/支付/退款聚成
    一个模块），布局权重压到最低档（手动逻辑关联也不会像手动外键那样拉扯骨架）。
  - 「含 FK 的 DDL」中逻辑关联以 `-- LOGICAL: a.col ~ b.col` 注释输出（绝不生成
    ALTER TABLE）。电商样例的 orders / payments / payment_refunds 新增
    `out_trade_no` 列（payments 侧唯一），首屏即可体验扫描。

- **字段评审批注**：画布上悬停字段行高亮、点击弹出批注气泡，写下的评审意见（命名 /
  类型 / 冗余 / 约束…）按字段持久化（刷新保留、导入新 SQL 清空），**并记录写下的
  时间**（气泡显示「写于 …」）；有批注的字段名旁显示琥珀色圆点。画布**右侧新增
  「评审建议」浮层**：最新在前列出全部批注（表.字段 + 时间 + 摘要），点击定位到
  对应表、逐条可删，支持折叠成小圆签。

- **两种评审产出物导出**：
  - **评审报告 (.md)**：显式外键 / 推断候选（含已接受、待定、已拒状态与理由）/
    逻辑关联 / 手动连线 / 字段评审意见（按表分组、带时间戳）/ 回收站排除说明——
    完整记录评审过程。
  - **数据库设计说明文档 (.md)**：标准格式——数据表概览、逐表字段定义表
    （序号 / 字段名 / 数据类型 / 允许空 / 默认值 / 键 / 说明）、主键与索引、
    逐表关系、关系汇总（外键 / 业务键逻辑关联分列）。只收录**已确认**的关系
    （DDL 声明 / 已接受推断 / 人工确认），待定候选归评审报告。

- **智能面板重组**：左侧面板按职责拆为平级区块——推断的外键（置信度分组 + 决策
  进度，仅物理外键候选）/ 逻辑关联 / 手动连线，原先全部塞在「推断的外键」里的混合
  结构不再存在。「逻辑关联」「手动连线」区块头部各有**「隐藏连线 / 显示连线」**
  开关，一键收起或放开整类连线（决策不受影响），方便对比「纯物理外键」与
  「全量关系」两种整体视图；导出跟随画布可见状态。

- 新增 [docs/roadmap.md](docs/roadmap.md)（四阶段价值挖掘计划）与
  [docs/handoff.md](docs/handoff.md)（交接文档）。

### 修复

- **同表自环连线在画布上不可见**：cytoscape 的 `segments` 曲线不渲染 self-loop，
  同表关系（如 `parent_id → id`）此前只出现在 SVG 导出里、画布上完全看不到。
  现在自环由 DOM 层按路由结果绘制**正交折线 U 形**，起点/终点严格停靠在两个字段行
  的中心、从绘制起手侧鼓出，画布与 SVG 导出完全一致。

- **绕行连线贴边**（拖动卡片后的实时布线）：左右两表连线绕过中间表时，此前绕行的
  竖直腿可能恰好贴着端点表格的边框下行、横向走廊与表格边缘齐平，视觉上分不清连线
  从哪个字段出发。现在绕行路由保证每个端口先水平伸出 18px 的短横段（stub）再拐弯，
  且把连线自己的两张端点卡片也纳入避让（10px 安全距），连线不再与任何卡片边框重合。

- **导出下拉菜单被评审建议浮层遮挡**：浮层层级降到工具栏下拉之下。

### 测试

- 测试规模从 204 用例增至 262 用例（30 个测试文件）：新增逻辑关联聚类/发现、
  手动连线校验与方向语义、无向键归一、持久化形状（含旧格式升级）、布线端口 stub、
  评审报告 / 说明文档 / DDL 注释导出等覆盖。

## [0.3.0] — 2026-07-07

一轮布线与布局质量改进：多条外键连线不再挤成一条线，稀疏 schema 导入不再是空白画布。

### 新增

- **连线自动分道**：新增全局后处理 `assignTracks`，把挤在同一分层间隙（gutter）里
  重合的平行外键连线聚簇后分配到各自车道，让每条连线各自成线、互不叠压。仅移动线段
  的内部段，手改路线 / 自环 / 批外边不受影响；若分道会穿过表体则回退原位（宁可重合，
  不穿表）。
- **布线质量度量**：新增纯函数 `routeMetrics`（正交路线的交叉数 / 重合数），作为布线
  改动的回归测试护栏，并供 `updateEdgeEndpoints` 的 DEV 日志实时输出当前布线数值。

### 修复

- **稀疏 schema 导入不再空白画布**：孤立表不再喂给 dagre —— 少 FK、多独立表时，dagre
  会把所有无边节点堆进 rank 0 的高列，抬高连通节点坐标并留出巨大竖向空白，相机居中后
  就是一片空白。现在只把跨表 FK 连通的子图交给 dagre，孤立表网格排在其下方（自环表按
  孤立处理，仍照常路由 U 形回环）。
- **大 schema 自动 fit**：自动适配的缩放下限从 `1.0` 降到 `0.2`，大于视口的 schema 改为
  「整体成帧优先」（此前钳在 1.0 只显示中间一小片，稀疏布局下那一片可能恰好是空的），
  以可读概览落地后用户再放大；工具栏「全览」仍是无钳制的真·适配全部。
- **手改连线不再随拖卡翻转**：拖动卡片期间实时逐帧重路由（`flushDrag`）忽略手动
  override，手改的连线不会因卡片中心越过原端口 x 而左右「啪」地跨过卡片
  （`DiagramCanvas.tsx` 新增 `nodeDraggingRef`）。

### 测试

- 新增 `assignTracks` / `routeMetrics` / `updateEdgeEndpoints` 测试；测试规模从 121 用例
  增至 204 用例（24 个测试文件）。

## [0.2.1] — 2026-06-01

源自 v0.2.0 画布特性的一轮代码评审修复：12 项正确性问题、若干清理，均经测试覆盖。

### 修复

- **搜索导航**：拖动卡片后匹配顺序按画布阅读顺序重排，活动游标在重排中保持指向同一张表；
  在防抖窗口内回车会等新匹配列表就绪后再跳转（不再步进旧列表）；输入时相机不再随每次按键乱跳
  （仅点选聚焦时居中，回车后才跟随当前匹配）。
- **撤销**：快照记录布线模式（`manualMove`），撤销回到初始布局时恢复静态 dagre 通道布线，
  而非实时绕行。
- **仅显示主键**：卡片高度与外键端口锚点正确按主键列计算（此前卡片过高、连线锚到空行；画布与 SVG 导出一致）。
- **手改连线**：稳定的内容派生 fkKey（不再因外键顺序变化而丢失手改路线）；拒绝空/退化/非有限路线；
  拖拽中跳过退化帧使屏上与持久化路线一致。
- **回收站 / 折叠**：`全部折叠` 跳过已回收的表（还原后不再意外折叠，也不再残留 sessionStorage 垃圾）。
- **画布生命周期**：卸载时清理进行中的拖拽监听与隐藏手柄定时器（避免在已销毁的 cy 上触发）；
  视口在刷新后逐字还原（精确复现上次画面）；外键决策不再清空撤销栈。
- 推断面板中 `_ref` 列的推断理由不再出现双重括号。

### 重构 / 清理

- 统一 `routePoints` 与端点字符串的编解码（`channelRoute`）；`nodeId` 提取为零依赖模块
  （store 不再牵入 cytoscape）；`undo`/`redo`、`manualRoutes` reducers、`PILL` 常量去重；
  提取可单测的纯函数 `clampPanAxis`；新增 `buildGraph` / `searchMatches` / `canvasSlice` / `clampPan` 测试。
- **持久化加固**：`persist` 增加 `migrate`（版本不匹配时仅保留 `rawSql`）与 `merge` 阶段的
  逐字段形状校验（每次加载都跑，丢弃畸形的 `nodePositions` / `manualRoutes` / `viewport` 等，
  回退到默认值），避免同版本下的形状漂移把错误布局直接喂进 store。

## [0.2.0] — 2026-06-01

自 `v0.1.0`（初始版本）以来的一轮大改：把画布从「只读自动布局」升级为
**可编辑、可保存的交互式画布**——多选/手型平移、连线可拖拽微调、表格可隐藏/还原、
搜索可跳转高亮、布局与视口跨刷新保存，并重写了底层布局与布线引擎、修复了暗色连线可见性。

### 画布交互与导航

- **框选多选**：在空白处拖拽拉出选框框选多张表；按住 `Shift` / `⌘` / `Ctrl`
  与已有选择求并集。小于 3px 的拖拽视为点击（清空选择）。
- **成组拖动**：选中 2 张以上时，拖动其中任意一张会以同一位移整体移动；
  拖动未选中的表则只移动那一张。
- **叠加点选**：`Shift` / `⌘` / `Ctrl` + 点击单张表，切换其在多选组中的成员身份。
- **多选浮标**：选中多张时底部中央出现计数浮标，可一键把整组丢进回收站；`Esc` 清空选择。
- **手型平移模式**：在「选择」与「手型」模式间切换；任意模式下按住 `Space` 左键拖拽即可平移，
  中键始终平移。平移做了内容边界钳制（始终保留 ≥120px 可见边距），且保持 1:1 跟手。
- **分级缩放**：缩放 `+/-` 按钮按固定档位吸附（25% / 33% / 50% / 67% / 75% / 100% / 125% / 150% /
  200% / 250% / 300% / 400% 共 12 档），并保持视口中心不动；触控板/⌘-滚轮缩放仍为连续无级。
- **视图菜单**：缩放百分比按钮展开快捷操作——缩放至 100% / 全览 / 缩放到选中 / 重置布局 / 全屏。
- **撤销 / 重做**：支持对卡片位置、宽度、手改连线的撤销重做（`⌘Z` / `⌘⇧Z` / `⌘Y`，
  最多 50 步，仅当前会话有效、不持久化）。
- **帮助浮层**：帮助按钮列出全部画布手势（框选、平移、拖动、修饰键多选、滚轮、撤销重做、Esc）。
- **控件重组**：布局/缩放/视图相关控件从顶部工具栏迁移到右下角悬浮的 `CanvasControls` 控件簇，
  更贴近视口操作。

### 连线（布线与编辑）

- **正交避障布线**：连线改为绕开卡片的正交走线——通畅时走「直角 2 弯（H-V-H）」，
  受阻时走可视图绕行（detour），上下堆叠的表走侧向括号布线（side-bracket）。
- **FK 直接连到主键**（修复）：修正了一处布线 bug——侧向括号曾把本应「直连」的同排两表
  的连线甩到远端边缘、并横穿目标表体；现在 FK 线头直接对准目标主键，在两表间走一条短折线。
- **拖动实时重布线**：拖动卡片时，相连的连线从当前端口实时重算，不再冻结在布局时的路点上，
  消除拖动产生的折角。
- **可拖拽折线手柄**：可点住任一中间线段的中点（蓝色圆点）垂直拖动该段，在保持正交结构的前提下
  手动微调走线。
- **手改走线保存**：手工调整过的连线路径会被记住，并在端口移动时按规则重新对接，
  布局引擎不再覆盖用户的手动编辑。

### 表格隐藏与回收站

- **删除即隐藏**：按 `Delete` / `Backspace`（或多选浮标的删除按钮）把选中的表从图中隐藏，
  **不改动 SQL**，只过滤视图。
- **回收站**：左下角回收站显示已隐藏数量，可逐个还原或一键全部还原。
- **导出同步**：被隐藏的表及其外键会从画布与 DDL / SVG / PNG 导出中一并排除。
- **空态提示**：当所有表都被隐藏时，画布中央提示从左下角回收站还原。

### 搜索

- **命中文本高亮**：搜索时除了高亮命中的整张表卡片，还会像 Chrome 查找一样，给命中的文本子串
  加黄色（amber）底色——覆盖表名、表注释、列名、列注释四处。
- **匹配导航**：搜索框旁显示「当前 / 总数」匹配计数；按 `回车` 跳到下一个匹配、`Shift+回车`
  上一个（也可点旁边的 ▲▼ 按钮），画布平移跟随当前命中的卡片，并给它加一圈更醒目的描边。
  匹配按画布阅读顺序（上→下、左→右）遍历。

### 外键推断

- **`_ref` 后缀识别**：除 `_id` / `Id` 外，FK 推断现在也识别 `_ref` / `Ref` 后缀（如 `credential_ref`）。驼峰形式要求大写字母前是小写字母，避免误拆 `href` / `XRef`。
- **同命名空间前缀匹配**：当裸列名（如 `credential`）匹配不到目标表时，会把来源表的命名空间前缀（如 `iam_credential_device` 的 `iam_`）拼到列名上再试一次，于是 `iam_credential_device.credential_ref` / `iam_challenge.credential_ref` 能正确指向 `iam_credential`（中等置信度）。这同样让既有 `_id` 列受益：电商样例里的 `warehouse_id → inventory_warehouses`、`campaign_id → marketing_campaigns` 现在也能推断出来。
- 电商样例新增 `payment_refunds.order_ref` 列，首屏即可看到 `_ref` 推断效果。

### 修复

- **暗色模式下连接线不可见**：FK 连接线按来源表的模块色（为浅色画布调校）着色，在暗色画布上
  深色配色（整套 `单色 mono`、`大地 earth` 的深棕/炭黑、`鲜艳 vibrant` 的偏深色）几乎看不见。
  现在暗色模式下会把过暗的连线颜色按 HSL 抬升到可见亮度（保留色相，已足够亮的 `粉彩 pastel`
  原样通过），画布与「暗色」SVG/PNG 导出都已覆盖；切回亮色恢复原色。

### 布局引擎

- **出版级分层布局**：用全新的、基于 dagre 的语义化「从左到右分层布局」`arrangeForPublication`
  取代原先的中心辐射式 `arrangeAroundHubs`（已删除），并预留分层间隙作为布线通道，
  让外键尽量笔直、尽量不交叉。
- **移除布局切换**：去掉了 力导向(fcose) / 分层(dagre) 的布局模式切换，统一为单一出版级布局。

### 持久化

- **布局跨刷新保存**：卡片位置、宽度覆盖、手改连线、回收站状态都会写入 `sessionStorage`，
  刷新页面后自动恢复。
- **画布视口跨刷新保存**（修复）：之前刷新虽然保住了布局，但画布的平移/缩放（相机）没有保存，
  刷新后相机回到原点，导致「刷新前后看到的屏幕不一致」。现在平移/缩放也会持久化，刷新后还原到
  完全相同的画面（节点位置 + 平移 + 缩放）。导入新 SQL / 重置布局 会重新自适应（fit）。
- **导入 vs 刷新**：导入新 SQL 会清空这些状态（全新开始）；刷新/重新解析则保留（保住手工布局与视口）。
- **重置布局**：「重置布局」按钮放弃手工调整（清除手改走线、重新自动布局并自适应视口），
  新布局与视口照常持久化；回收站内容不受影响。

### 导出

- **精确连线导出**：SVG / PNG 导出改为直接使用画布上的实际 `routePoints`，
  与屏幕所见一致（包含手动调整过的折弯）。
- **导出遵循可见状态**：DDL / 图片导出只包含当前可见内容，回收站中的表与其外键被排除。

### 选中态视觉

- **多选高亮**：进入多选组的卡片显示天蓝色描边；搜索/聚焦命中仍为琥珀色描边；两者可与置灰共存。
- **平移穿透**：手型模式下卡片变为「点击穿透」（`pointer-events: none`），左键拖拽平移画布而非拖动卡片。

### 内部 / 重构

- 布线逻辑收敛为 `channelRoute` 单一可组合模块（含纯函数与单测）；删除旧的 `computeSegments`。
- `cyHandle` 扩展为同时管理 cy 实例、命令式视图 API（缩放/平移/重排/`centerOnNode`）与会话级撤销重做栈。
- Store 拆分为 slice：`schema` / `decisions` / `display` / `canvas` / `history`；
  新增 `visibleSchema` 选择器作为「画布/导出实际可见内容」的单一事实源；
  `canvas` slice 新增 `viewport`（画布相机 = 平移/缩放）持久化字段，与 `nodePositions` 同生命周期；
  `buildGraph` 为每条外键生成稳定的 `fkKey`，使手改走线能跨重建存活。
- 新增纯函数 / 工具模块：`highlight`（命中文本高亮）、`edgeColor`（暗色连线 HSL 亮度抬升）、
  `searchNav`（匹配导航环绕计算）；新增 `useResolvedTheme` 响应式主题 hook，供 canvas 在主题
  切换时给连线重新着色。
- 依赖从 `cytoscape-fcose` / `cytoscape-dagre` 插件切换为直接依赖 `@dagrejs/dagre`。
- 新增手型/缩放/撤销/重做/帮助/删除/还原等图标；补充平移光标与框选样式。

### 测试

- 测试套件扩充至 16 个文件、140 个用例（新增 `marquee`、`dragGroup`、`channelRoute`、
  `arrangeForPublication`、`cyHandle`、`searchNav`、`edgeColor`、`viewport` 等用例）。
- 新增 `vitest.setup.ts`：为 node 测试环境提供内存版 `sessionStorage`，让 store 持久化相关用例
  输出干净（不再打印「storage unavailable」告警）。

## [0.1.0] — 2026-05-25

- 首个版本：粘贴/上传 SQL DDL，解析为可交互 ER 图；当脚本未声明外键时，启发式推断 FK 关系。
