# Changelog

本文件记录 ER Diagram Viewer 的重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/)，
版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 修复

- **手改连线不再随拖卡翻转**：拖动卡片期间，实时逐帧重路由（`flushDrag`）忽略手动
  override，手改的连线不会因卡片中心越过原端口 x 而左右「啪」地跨过卡片
  （`DiagramCanvas.tsx` 新增 `nodeDraggingRef`）。轻量实现，不改持久化结构、不升版本。

### 计划中（altitude，P2）

- 布线引擎统一（始终走 `detourRoute`、退役 dagre 通道航点）、side-bracket 泛化。
  详见 `TODO-fix-bugs.md`。

## [0.2.1] — 2026-06-01

源自 v0.2.0 画布特性的一轮代码评审修复：12 项正确性问题、若干清理，均经测试覆盖。

### 修复

- **搜索导航**：拖动卡片后匹配顺序按画布阅读顺序重排，活动游标在重排中保持指向同一张表；
  在防抖窗口内回车会等新匹配列表就绪后再跳转（不再步进旧列表）；输入时相机不再随每次按键乱跳
  （仅点选聚焦时居中，回车后才跟随当前匹配）。
- **撤销**：快照记录布线模式（`manualMove`），撤销回到初始布局时恢复静态 dagre 通道布线，
  而非实时绕行。
- **仅显示主键**：卡片高度与外键端口锚点正确按主键列计算（此前卡片过高、连线锚到空行；画布与 SVG 导出一致）。
- **手改连线**：稳定的内容派生 fkKey（不再因外键顺序变化而丢失手改路线）；拒绝空/退化/非有限路线；
  拖拽中跳过退化帧使屏上与持久化路线一致。
- **回收站 / 折叠**：`全部折叠` 跳过已回收的表（还原后不再意外折叠，也不再残留 sessionStorage 垃圾）。
- **画布生命周期**：卸载时清理进行中的拖拽监听与隐藏手柄定时器（避免在已销毁的 cy 上触发）；
  视口在刷新后逐字还原（精确复现上次画面）；外键决策不再清空撤销栈。
- 推断面板中 `_ref` 列的推断理由不再出现双重括号。

### 重构 / 清理

- 统一 `routePoints` 与端点字符串的编解码（`channelRoute`）；`nodeId` 提取为零依赖模块
  （store 不再牵入 cytoscape）；`undo`/`redo`、`manualRoutes` reducers、`PILL` 常量去重；
  提取可单测的纯函数 `clampPanAxis`；新增 `buildGraph` / `searchMatches` / `canvasSlice` / `clampPan` 测试。
- **持久化加固**：`persist` 增加 `migrate`（版本不匹配时仅保留 `rawSql`）与 `merge` 阶段的
  逐字段形状校验（每次加载都跑，丢弃畸形的 `nodePositions` / `manualRoutes` / `viewport` 等，
  回退到默认值），避免同版本下的形状漂移把错误布局直接喂进 store。

## [0.2.0] — 2026-06-01

自 `v0.1.0`（初始版本）以来的一轮大改：把画布从「只读自动布局」升级为
**可编辑、可保存的交互式画布**——多选/手型平移、连线可拖拽微调、表格可隐藏/还原、
搜索可跳转高亮、布局与视口跨刷新保存，并重写了底层布局与布线引擎、修复了暗色连线可见性。

### 画布交互与导航

- **框选多选**：在空白处拖拽拉出选框框选多张表；按住 `Shift` / `⌘` / `Ctrl`
  与已有选择求并集。小于 3px 的拖拽视为点击（清空选择）。
- **成组拖动**：选中 2 张以上时，拖动其中任意一张会以同一位移整体移动；
  拖动未选中的表则只移动那一张。
- **叠加点选**：`Shift` / `⌘` / `Ctrl` + 点击单张表，切换其在多选组中的成员身份。
- **多选浮标**：选中多张时底部中央出现计数浮标，可一键把整组丢进回收站；`Esc` 清空选择。
- **手型平移模式**：在「选择」与「手型」模式间切换；任意模式下按住 `Space` 左键拖拽即可平移，
  中键始终平移。平移做了内容边界钳制（始终保留 ≥120px 可见边距），且保持 1:1 跟手。
- **分级缩放**：缩放 `+/-` 按钮按固定档位吸附（25% / 33% / 50% / 67% / 75% / 100% / 125% / 150% /
  200% / 250% / 300% / 400% 共 12 档），并保持视口中心不动；触控板/⌘-滚轮缩放仍为连续无级。
- **视图菜单**：缩放百分比按钮展开快捷操作——缩放至 100% / 全览 / 缩放到选中 / 重置布局 / 全屏。
- **撤销 / 重做**：支持对卡片位置、宽度、手改连线的撤销重做（`⌘Z` / `⌘⇧Z` / `⌘Y`，
  最多 50 步，仅当前会话有效、不持久化）。
- **帮助浮层**：帮助按钮列出全部画布手势（框选、平移、拖动、修饰键多选、滚轮、撤销重做、Esc）。
- **控件重组**：布局/缩放/视图相关控件从顶部工具栏迁移到右下角悬浮的 `CanvasControls` 控件簇，
  更贴近视口操作。

### 连线（布线与编辑）

- **正交避障布线**：连线改为绕开卡片的正交走线——通畅时走「直角 2 弯（H-V-H）」，
  受阻时走可视图绕行（detour），上下堆叠的表走侧向括号布线（side-bracket）。
- **FK 直接连到主键**（修复）：修正了一处布线 bug——侧向括号曾把本应「直连」的同排两表
  的连线甩到远端边缘、并横穿目标表体；现在 FK 线头直接对准目标主键，在两表间走一条短折线。
- **拖动实时重布线**：拖动卡片时，相连的连线从当前端口实时重算，不再冻结在布局时的路点上，
  消除拖动产生的折角。
- **可拖拽折线手柄**：可点住任一中间线段的中点（蓝色圆点）垂直拖动该段，在保持正交结构的前提下
  手动微调走线。
- **手改走线保存**：手工调整过的连线路径会被记住，并在端口移动时按规则重新对接，
  布局引擎不再覆盖用户的手动编辑。

### 表格隐藏与回收站

- **删除即隐藏**：按 `Delete` / `Backspace`（或多选浮标的删除按钮）把选中的表从图中隐藏，
  **不改动 SQL**，只过滤视图。
- **回收站**：左下角回收站显示已隐藏数量，可逐个还原或一键全部还原。
- **导出同步**：被隐藏的表及其外键会从画布与 DDL / SVG / PNG 导出中一并排除。
- **空态提示**：当所有表都被隐藏时，画布中央提示从左下角回收站还原。

### 搜索

- **命中文本高亮**：搜索时除了高亮命中的整张表卡片，还会像 Chrome 查找一样，给命中的文本子串
  加黄色（amber）底色——覆盖表名、表注释、列名、列注释四处。
- **匹配导航**：搜索框旁显示「当前 / 总数」匹配计数；按 `回车` 跳到下一个匹配、`Shift+回车`
  上一个（也可点旁边的 ▲▼ 按钮），画布平移跟随当前命中的卡片，并给它加一圈更醒目的描边。
  匹配按画布阅读顺序（上→下、左→右）遍历。

### 外键推断

- **`_ref` 后缀识别**：除 `_id` / `Id` 外，FK 推断现在也识别 `_ref` / `Ref` 后缀（如 `credential_ref`）。驼峰形式要求大写字母前是小写字母，避免误拆 `href` / `XRef`。
- **同命名空间前缀匹配**：当裸列名（如 `credential`）匹配不到目标表时，会把来源表的命名空间前缀（如 `iam_credential_device` 的 `iam_`）拼到列名上再试一次，于是 `iam_credential_device.credential_ref` / `iam_challenge.credential_ref` 能正确指向 `iam_credential`（中等置信度）。这同样让既有 `_id` 列受益：电商样例里的 `warehouse_id → inventory_warehouses`、`campaign_id → marketing_campaigns` 现在也能推断出来。
- 电商样例新增 `payment_refunds.order_ref` 列，首屏即可看到 `_ref` 推断效果。

### 修复

- **暗色模式下连接线不可见**：FK 连接线按来源表的模块色（为浅色画布调校）着色，在暗色画布上
  深色配色（整套 `单色 mono`、`大地 earth` 的深棕/炭黑、`鲜艳 vibrant` 的偏深色）几乎看不见。
  现在暗色模式下会把过暗的连线颜色按 HSL 抬升到可见亮度（保留色相，已足够亮的 `粉彩 pastel`
  原样通过），画布与「暗色」SVG/PNG 导出都已覆盖；切回亮色恢复原色。

### 布局引擎

- **出版级分层布局**：用全新的、基于 dagre 的语义化「从左到右分层布局」`arrangeForPublication`
  取代原先的中心辐射式 `arrangeAroundHubs`（已删除），并预留分层间隙作为布线通道，
  让外键尽量笔直、尽量不交叉。
- **移除布局切换**：去掉了 力导向(fcose) / 分层(dagre) 的布局模式切换，统一为单一出版级布局。

### 持久化

- **布局跨刷新保存**：卡片位置、宽度覆盖、手改连线、回收站状态都会写入 `sessionStorage`，
  刷新页面后自动恢复。
- **画布视口跨刷新保存**（修复）：之前刷新虽然保住了布局，但画布的平移/缩放（相机）没有保存，
  刷新后相机回到原点，导致「刷新前后看到的屏幕不一致」。现在平移/缩放也会持久化，刷新后还原到
  完全相同的画面（节点位置 + 平移 + 缩放）。导入新 SQL / 重置布局 会重新自适应（fit）。
- **导入 vs 刷新**：导入新 SQL 会清空这些状态（全新开始）；刷新/重新解析则保留（保住手工布局与视口）。
- **重置布局**：「重置布局」按钮放弃手工调整（清除手改走线、重新自动布局并自适应视口），
  新布局与视口照常持久化；回收站内容不受影响。

### 导出

- **精确连线导出**：SVG / PNG 导出改为直接使用画布上的实际 `routePoints`，
  与屏幕所见一致（包含手动调整过的折弯）。
- **导出遵循可见状态**：DDL / 图片导出只包含当前可见内容，回收站中的表与其外键被排除。

### 选中态视觉

- **多选高亮**：进入多选组的卡片显示天蓝色描边；搜索/聚焦命中仍为琥珀色描边；两者可与置灰共存。
- **平移穿透**：手型模式下卡片变为「点击穿透」（`pointer-events: none`），左键拖拽平移画布而非拖动卡片。

### 内部 / 重构

- 布线逻辑收敛为 `channelRoute` 单一可组合模块（含纯函数与单测）；删除旧的 `computeSegments`。
- `cyHandle` 扩展为同时管理 cy 实例、命令式视图 API（缩放/平移/重排/`centerOnNode`）与会话级撤销重做栈。
- Store 拆分为 slice：`schema` / `decisions` / `display` / `canvas` / `history`；
  新增 `visibleSchema` 选择器作为「画布/导出实际可见内容」的单一事实源；
  `canvas` slice 新增 `viewport`（画布相机 = 平移/缩放）持久化字段，与 `nodePositions` 同生命周期；
  `buildGraph` 为每条外键生成稳定的 `fkKey`，使手改走线能跨重建存活。
- 新增纯函数 / 工具模块：`highlight`（命中文本高亮）、`edgeColor`（暗色连线 HSL 亮度抬升）、
  `searchNav`（匹配导航环绕计算）；新增 `useResolvedTheme` 响应式主题 hook，供 canvas 在主题
  切换时给连线重新着色。
- 依赖从 `cytoscape-fcose` / `cytoscape-dagre` 插件切换为直接依赖 `@dagrejs/dagre`。
- 新增手型/缩放/撤销/重做/帮助/删除/还原等图标；补充平移光标与框选样式。

### 测试

- 测试套件扩充至 16 个文件、140 个用例（新增 `marquee`、`dragGroup`、`channelRoute`、
  `arrangeForPublication`、`cyHandle`、`searchNav`、`edgeColor`、`viewport` 等用例）。
- 新增 `vitest.setup.ts`：为 node 测试环境提供内存版 `sessionStorage`，让 store 持久化相关用例
  输出干净（不再打印「storage unavailable」告警）。

## [0.1.0] — 2026-05-25

- 首个版本：粘贴/上传 SQL DDL，解析为可交互 ER 图；当脚本未声明外键时，启发式推断 FK 关系。
