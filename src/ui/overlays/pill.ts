/** Shared "floating pill" container styling for the bottom overlay clusters
 *  (recycle bin, canvas controls) so they stay visually identical. */
export const PILL =
  'flex items-center rounded-full px-1 border border-ink-200 dark:border-inkd-300 ' +
  'bg-white/90 dark:bg-inkd-100/90 backdrop-blur shadow-lg';
