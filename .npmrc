# Pin registry at the project level so `bun install` (and any incidental npm
# usage) doesn't fall through to a global mirror that may be missing modern
# dev dependencies like @typescript-eslint/*. Bun reads this file too.
registry=https://registry.npmjs.org/
